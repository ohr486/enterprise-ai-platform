# enterprise-ai-platform

エンタープライズ向け AI エージェントプラットフォーム（設計検討用リポジトリ）。

> **本リポジトリは設計情報をリセットした直後の状態です。** 既存の要件定義書・実装プラン・ADR は意図的に削除されています。今後の設計ドキュメントはゼロから再構築します。

## 現在の状態

- `docs/` 配下の設計資料（REQUIREMENTS / PLAN / ADR）は削除済み
- アプリケーションコード・インフラ・テストは未着手
- リポジトリには本ファイル、`CLAUDE.md`、`.gitignore`、参照用シンボリックリンクのみが残っています

## リポジトリ構成（現状）

```
.
├── docs/                        # 空（設計資料を再構築予定）
├── README.md                    # 本ファイル
├── CLAUDE.md                    # Claude Code 向けガイダンス
├── .gitignore
├── sample/   → シンボリックリンク（gitignored、参照用 AWS Samples）
└── ecc/      → シンボリックリンク（gitignored、Claude plugin）
```

## 次のステップ

設計をゼロから組み直す場合は、対象サブシステムのスコープ・要件・制約を改めて整理したうえで、`docs/` 配下に新規ドキュメントを起票してください。
