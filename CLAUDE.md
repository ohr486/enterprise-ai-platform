# CLAUDE.md

このファイルは、Claude Code (claude.ai/code) が本リポジトリで作業する際の指針を提供します。

## リポジトリの状態

**本リポジトリは設計情報をリセットした直後の状態です。** `docs/` 配下にあった要件定義書（REQUIREMENTS）・実装プラン（PLAN）・ADR は意図的に削除されています。アプリケーションコード・インフラ・テストも未着手です。

そのため、ビルド・lint・テストの実行コマンドは存在しません。コードまたは設計資料を追加する際は、選定した言語・ツール・方針に応じた実コマンドを本ファイルに追記してください。

## 設計再構築時の方針

設計をゼロから組み直す際は、以下のいずれかの方針で進めてください:

- スコープ・要件・制約をユーザーと合意したうえで `docs/` 配下に新規ドキュメントを起票する
- 要件（What / Why / 制約 / 受け入れ基準）と実装（How / フェーズ / コード / Terraform）を分離するかどうかも改めて決める

過去の設計（OPENCLAW 本体・MCP Gateway・Knowledge DB の三層構成、500 ユーザー上限、Slack 専用、3 層 SOUL、4 ティアランタイム、5 層多層防御など）は削除済みです。以前の設計を踏襲するかどうかも、再着手時にユーザー確認が必要です。

## リポジトリ構成

```
.
├── docs/                        # 空（設計資料は削除済み）
├── README.md                    # 概要
├── CLAUDE.md                    # 本ファイル
├── .gitignore                   # sample/ と ecc/ シンボリックリンクを除外
├── sample/   → シンボリックリンク（gitignored、参照用 AWS Samples）
└── ecc/      → シンボリックリンク（gitignored、Claude plugin）
```

## 運用上の注意

- ユーザーの母語は日本語です。設計ドキュメントの議論・コメント・コミットメッセージなどは日本語を優先してください。
- 設計資料が存在しない状態で実装スコープに踏み込む要望が来た場合、まず要件・スコープの合意プロセスを提案してください。
